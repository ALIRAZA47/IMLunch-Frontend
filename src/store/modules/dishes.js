const state = {};
const getters = {
  getMenu: (state) => () => {
    return state.menu;
  },
};
const mutations = {};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
